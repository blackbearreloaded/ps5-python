# LAN File Browser

Read-only local-network file browser. It serves files only from
`/data/python/share` by default and supports safe downloads. Set
`CPYTHONPS5_SHARE_ROOT` to an approved shared directory.

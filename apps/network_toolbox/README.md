# Network Toolbox

LAN-friendly DNS, TCP connectivity, and HTTP status checks. Enter a hostname
or URL in the browser at port `9104` to exercise the PS5 socket and networking
stack from a useful diagnostic tool.

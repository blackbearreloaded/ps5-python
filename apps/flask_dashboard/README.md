# Flask PS5 Dashboard

An actual Flask service for the PS5. It shows runtime information, the app
bundle location, the current time, and a JSON health endpoint on port `9101`.

Open `http://PS5_IP:9101/` after launching it from the Python manager.

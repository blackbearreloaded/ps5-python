# Storage Inspector

Read-only browser for `/data/python` by default. It lists directories and
files with sizes and modification times without allowing deletion or access
outside the configured root. Set `PYTHON_PS5_STORAGE_ROOT` to inspect another
approved PS5 directory.

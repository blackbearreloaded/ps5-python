# Markdown Documentation

Serves local Markdown documentation as a safe, escaped HTML page without an
extra Markdown dependency. It uses `/data/python/docs` when present and ships
with a small fallback document.

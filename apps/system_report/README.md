# System Report

A regular process-backed Python script rather than a web server. It prints
runtime details and the arguments supplied in the launcher.

Try arguments such as `--source demo --verbose` from the Applications card.
